# ScreenShot.py 仕様書

## 1. 概要

本スクリプト (`ScreenShot.py`) は、指定されたウィンドウのスクリーンショットを連続して撮影し、指定された矢印キーでページをめくる操作を自動化するPythonスクリプトである。主に電子書籍リーダーアプリケーション（例: Kindle）などのページ送り操作と画面キャプチャを効率化することを目的とする。

## 2. 機能要件

*   **ウィンドウ特定**: コマンドライン引数で指定されたタイトル文字列を含むウィンドウを検索し、操作対象とする。
*   **スクリーンショット撮影**: 特定したウィンドウの表示領域全体のスクリーンショットを指定された回数撮影する。
*   **ページめくり**: 各スクリーンショット撮影後、指定された矢印キー（左、右、上、下）を押下してページ送りを行う。
*   **ファイル保存**: 撮影したスクリーンショットを、指定されたフォルダに連番付きのPNGファイルとして保存する (`page_XXX.png`)。
*   **待機処理**: ページめくり操作後に、指定された秒数だけ待機する。
*   **コマンドラインインターフェース**: 動作に必要なパラメータ（対象ウィンドウタイトル、ページめくり方向、撮影枚数、待機時間、保存先フォルダ）をコマンドライン引数で受け付ける。
*   **エラーハンドリング**: 対象ウィンドウが見つからない場合、撮影中にウィンドウが閉じた場合、ユーザーによる中断 (Ctrl+C) などを適切に処理する。

## 3. ScreenShot.py インターフェース (コマンドライン引数)

`argparse` ライブラリを使用し、以下のコマンドライン引数を受け付ける。

| 引数名          | オプション         | 型      | デフォルト値    | 説明                                                                   |
|-----------------|------------------|---------|---------------|------------------------------------------------------------------------|
| `direction`     | `--direction`    | `str`   | `'left'`      | ページめくりに使用する矢印キー (`left`, `right`, `up`, `down`)。         |
| `title`         | `--title`        | `str`   | `'Kindle'`    | 操作対象ウィンドウのタイトルに含まれる文字列。                         |
| `count`         | `--count`        | `int`   | `10`          | 撮影するスクリーンショットの枚数。                                     |
| `delay`         | `--delay`        | `float` | `1.0`         | ページめくり後の待機時間（秒）。                                       |
| `output`        | `--output`       | `str`   | `'screenshots'` | スクリーンショットの保存先フォルダ名。存在しない場合は自動作成。         |

## 4. ScreenShot.py 使用ライブラリ・モジュール

*   `pyautogui`: GUI操作（スクリーンショット撮影、キーボード入力）
*   `time`: 待機処理 (`sleep`)
*   `os`: ファイルシステム操作（フォルダ作成、パス結合）
*   `pygetwindow`: ウィンドウ情報の取得（タイトル検索、位置、サイズ）
*   `argparse`: コマンドライン引数の解析

## 5. ScreenShot.py 実行方法

以下の形式でコマンドを実行する。

```bash
python ScreenShot.py [オプション]
```

**例:**

```bash
python ScreenShot.py --title "My App" --direction right --count 100 --delay 1.2 --output captures
```

## 6. ScreenShot.py 前提条件・制約事項

*   **ライブラリのインストール**: 上記「4. ScreenShot.py 使用ライブラリ・モジュール」に記載のPythonライブラリ (`pyautogui`, `pygetwindow`) がインストールされている必要がある。
*   **対象ウィンドウ**: スクリプト実行時に、対象となるウィンドウ（`--title` で指定）が開かれている必要がある。
*   **ウィンドウのアクティブ化**: ページめくり操作 (`pyautogui.press()`) が正しく機能するためには、対象ウィンドウがキーボードフォーカスを持っている（アクティブである）ことが望ましい。
*   **ウィンドウサイズ**: 対象ウィンドウが最小化されているなど、幅または高さが0以下の場合はエラーとなり処理を中断する。
*   **macOSの座標**: macOSの高解像度ディスプレイ環境では、`pyautogui` が取得する座標と実際のピクセル座標が異なる場合があり、スクリーンショット領域がずれる可能性がある。
*   **排他制御**: 本スクリプトは、他のプロセスが同時に対象ウィンドウやマウス/キーボードを操作することは想定していない。

## 7. ScreenShot.py エラー処理

*   **ウィンドウ不検出**: `--title` で指定されたウィンドウが見つからない場合、エラーメッセージを表示して終了する。
*   **ウィンドウ消失/不正**: スクリーンショット撮影ループ中に、対象ウィンドウが閉じられたり、サイズが取得できなくなったりした場合、エラーメッセージを表示して処理を中断する。
*   **キーボード割り込み**: 実行中に `Ctrl+C` が押された場合、`KeyboardInterrupt` を捕捉し、メッセージを表示して正常に終了する。
*   **その他**: 上記以外の予期せぬエラーが発生した場合も、例外を捕捉しエラーメッセージを表示する。

## 8. ScreenShot.py 出力

*   **フォルダ**: `--output` で指定されたフォルダが作成される（存在しない場合）。
*   **PNGファイル**: 指定されたフォルダ内に、`page_001.png`, `page_002.png`, ... の形式でスクリーンショット画像ファイル（PNG形式）が保存される。

---

# PdfMaker.py 仕様書

## 9. 概要

本スクリプト (`PdfMaker.py`) は、指定されたフォルダ内にある連番形式のPNG画像ファイル (`page_XXX.png`) を検索し、ファイル名の数値に基づいてソートした後、指定された範囲の画像を結合して単一のPDFファイルを作成する独立したコマンドラインツールである。

## 10. 機能要件

*   **PNGファイル検索**: 指定された入力フォルダ内から `page_*.png` のパターンに一致するファイルを検索する。
*   **自然順ソート**: 見つかったPNGファイルを、ファイル名に含まれる数値に基づいて自然順（例: `page_1.png`, `page_2.png`, ..., `page_10.png`）にソートする。
*   **範囲指定**: コマンドライン引数で指定された開始番号（1始まり）と枚数に基づき、ソート済みリストからPDFに含めるPNGファイルの範囲を選択する。
*   **PDF生成**: 選択された範囲のPNGファイルを順番に結合し、指定された出力ファイルパスにPDFファイルとして保存する。
*   **コマンドラインインターフェース**: 入力フォルダ、出力PDFファイルパス、開始番号、枚数をコマンドライン引数で受け付ける。
*   **エラーハンドリング**: 入力フォルダやファイルが見つからない場合、指定範囲が無効な場合、アルファチャンネル付きPNGによるエラー、その他のPDF生成エラーなどを適切に処理する。

## 11. PdfMaker.py インターフェース (コマンドライン引数)

`argparse` ライブラリを使用し、以下のコマンドライン引数を受け付ける。

| 引数名      | オプション   | 型     | デフォルト値  | 説明                                                            |
|-------------|--------------|--------|-------------|-----------------------------------------------------------------|
| `input`     | `-i`, `--input` | `str`  | (必須)      | 入力PNGファイル (`page_*.png`) が含まれるフォルダへのパス。       |
| `output`    | `-o`, `--output`| `str`  | `output.pdf`| 出力するPDFのファイルパス。                                     |
| `start`     | `--start`    | `int`  | `1`         | PDFに含める最初のPNGファイルの番号 (1始まり)。                    |
| `count`     | `--count`    | `int`  | `10`        | PDFに含めるPNGファイルの枚数。                                  |

## 12. PdfMaker.py 使用ライブラリ

*   `img2pdf`: PNG画像リストからPDFを生成
*   `os`: パス操作
*   `glob`: ファイルパターンマッチング
*   `re`: 正規表現（自然順ソート用）
*   `argparse`: コマンドライン引数の解析
*   `Pillow`: `img2pdf` が内部で使用 (暗黙的な依存)

## 13. PdfMaker.py 実行方法

以下の形式でコマンドを実行する。

```bash
python PdfMaker.py --input <入力フォルダ> [オプション]
```

**例:**

```bash
# screenshots フォルダ内の page_*.png からデフォルト設定 (1番目から10枚) で output.pdf を作成
python PdfMaker.py --input screenshots 

# my_captures フォルダ内の page_*.png のうち、6番目から20枚を my_document.pdf として作成
python PdfMaker.py --input my_captures --output my_document.pdf --start 6 --count 20
```

## 14. PdfMaker.py 前提条件・制約事項

*   **ライブラリのインストール**: 上記「12. PdfMaker.py 使用ライブラリ」に記載のPythonライブラリ (`img2pdf`, `Pillow`) がインストールされている必要がある。
*   **入力ファイル形式**: 入力フォルダには `page_XXX.png` のような、数値を含むファイル名を持つPNGファイルが存在することを想定している。
*   **アルファチャンネル**: `img2pdf` はアルファチャンネル（透過情報）を持つPNGの結合に対応していないため、そのようなファイルがあるとエラーになる。

## 15. PdfMaker.py エラー処理

*   **入力フォルダ/ファイル不検出**: 指定された入力フォルダが存在しない、またはフォルダ内に `page_*.png` ファイルが見つからない場合、エラーメッセージを表示して終了する。
*   **範囲指定エラー**: `--start` や `--count` で指定された範囲が、見つかったファイル数に対して無効な場合（開始番号が大きすぎるなど）、エラーメッセージを表示して終了する。
*   **アルファチャンネルエラー**: 透過情報を持つPNGファイルが含まれていた場合、`img2pdf.AlphaChannelError` を捕捉し、エラーメッセージを表示して終了する。
*   **その他**: その他のファイルIOエラーや予期せぬエラーが発生した場合も、例外を捕捉しエラーメッセージを表示する。

## 16. PdfMaker.py 出力

*   **PDFファイル**: `--output` で指定されたパスに、結合されたPDFファイルが生成される。 